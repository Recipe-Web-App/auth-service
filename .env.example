# OAuth2 Authentication Service Environment Configuration
# Copy this file to .env.local for local development or .env.prod for production deployment
#
# This file contains ONLY environment-specific connection data and secrets.
# All operational settings (timeouts, pool sizes, etc.) are managed in configs/*.yaml files.

# ============================================================================
# ENVIRONMENT CONFIGURATION
# ============================================================================
# Determines which YAML config to load (LOCAL, NONPROD, or PROD)
ENVIRONMENT=LOCAL

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# ============================================================================
# JWT CONFIGURATION (REQUIRED - SENSITIVE)
# ============================================================================
# Generate a secure 32+ character secret key - NEVER commit this value
JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================
REDIS_HOST=redis-database.local
REDIS_PORT=6379
REDIS_DB=0
REDIS_USER=auth_user
REDIS_PASSWORD=

# ============================================================================
# POSTGRESQL DATABASE CONFIGURATION (Optional)
# ============================================================================
# If not configured, user data will only be stored in Redis (not persisted)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=recipe_database
POSTGRES_SCHEMA=recipe_manager
POSTGRES_USER=                          # REQUIRED if using PostgreSQL
POSTGRES_PASSWORD=                      # REQUIRED if using PostgreSQL

# ============================================================================
# MYSQL DATABASE CONFIGURATION (Optional)
# ============================================================================
# If not configured, OAuth2 clients will only be stored in Redis
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=client_manager
MYSQL_CLIENT_DB_USER=                   # REQUIRED if using MySQL
MYSQL_CLIENT_DB_PASSWORD=               # REQUIRED if using MySQL

# ============================================================================
# AUTH SERVICE CLIENT CREDENTIALS
# ============================================================================
AUTH_SERVICE_CLIENT_ID=auth-service-client-id
AUTH_SERVICE_CLIENT_SECRET=auth-service-client-secret
